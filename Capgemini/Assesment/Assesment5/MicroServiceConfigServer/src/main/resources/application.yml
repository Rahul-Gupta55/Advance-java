server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rahul-Gupta55/microservices-config.git
          clone-on-start: true